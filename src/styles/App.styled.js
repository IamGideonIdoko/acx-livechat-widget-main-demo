import styled from 'styled-components';
import { priColor } from './variables';

export const Container = styled.div `
    max-width:  375px;
    min-height: 600px;
    background: ${priColor};
    color: white;
    margin: 0 auto;
`;